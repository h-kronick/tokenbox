{
  "name": "tokenbox-windows",
  "version": "1.0.0",
  "private": true,
  "description": "TokenBox — Split-flap display for Claude token usage (Windows/cross-platform)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
